# g1f4 M protocol amendment v1

schema: `g1f4_m_protocol_amendment_v1`

This immutable amendment does not modify or replace the frozen M-v1
preregistration JSON. It records the protocol ordering before any new M-only
or PM result is produced.

## Reason

The completed P full-train and residual audit show that the progress-policy
blockage improved, while stable positive hard-Guard geometry residuals remain
after the k5 budget. This authorizes a separately bounded metric factorial
train experiment. It does not alter the metric definition, the Euclidean
baseline radius, budgets, angle ladder, Guard, Projector, ownership, taper,
Adapter incumbent contract, or the P policy.

## Amended sequence

```text
M preregistration
→ contract repair
→ train-only M calibration
→ canonical v9 parity
→ M-only
→ PM
→ cold-start replication
→ frozen development validation
→ held-out (requires separate authorization)
```

The amendment must be SHA256-bound to the run before M calibration starts. It
does not consume development or held-out evidence, and future M-only or PM
results cannot be used to modify the metric definition or this amendment.
